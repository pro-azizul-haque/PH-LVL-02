//input: [2, 5, 8, 12, 15, 18, 22, 27, 30, 33, 37, 41]
// output: 
 binned = {  '0-9': [ 2, 5, 8 ],
  '10-19': [ 12, 15, 18 ],
  '20-29': [ 22, 27 ],
  '30-39': [ 30, 33, 37 ],
  '40-49': [ 41 ]
}
/**
Binning (বিনিং) হল একটি ডেটা প্রসেসিং বা অ্যানালাইসিস টেকনিক,
 যেখানে ধারাবাহিক (continuous) বা সংখ্যাগত ডেটাকে নির্দিষ্ট range বা “bin”-এ ভাগ করা হয়।
👉 সহজভাবে বললে, একটা বড় ডেটাকে ছোট ছোট ভাগে বা bucket-এ ভাগ করা — একে বলে binning।
 */

function binData(data, binRange) {
  const bins = {};

  for (const value of data) {
    // main concept hocche current number er place-value koto ber kora mathemathically tarpor simply kaj kora
    // value ta kun bin porbe ta ber kora
    const binStart = Math.floor(value / binRange) * binRange; // get the decimal position
    const binEnd = binStart + binRange - 1;
    const binLabel = `${binStart}-${binEnd}`
    if(!bins[binLabel]){
        bins[binLabel] = []
    }
    bins[binLabel].push(value)
  }
  console.log(bins);
  return bins
}

const data = [2, 5, 8, 12, 15, 18, 22, 27, 30, 33, 37, 41];

// Bin range নির্ধারণ (প্রতিটা bin এর width = 10)
const binSize = 10;
binData(data,binSize)


// Logic Explanation
/*
একদম ঠিক জায়গায় প্রশ্ন করছ ভাই 💪
চলো ধাপে ধাপে **logicটা ভেঙে** বোঝাই — যেন তুমি একদম ক্লিয়ারলি বুঝতে পারো প্রতিটা লাইন কী করছে 👇

---

### 🔹 ধরো আমাদের data:

```js
const data = [2, 5, 8, 12, 15, 18, 22];
const binSize = 10;
```

মানে আমরা চাই প্রতি 10 রেঞ্জে একটা bin (0–9, 10–19, 20–29 ...)

---

## 🧩 Step-by-step breakdown

### ① `for (let value of data)`

👉 আমরা প্রতিটা সংখ্যার উপর লুপ চালাচ্ছি
মানে — ২, তারপর ৫, তারপর ৮, তারপর ১২ ... একে একে নিবো।

---

### ② `Math.floor(value / binSize)`

এখানে আমরা বুঝতে চাই **value কোন bin এ পড়বে**।

* যদি `value = 2` হয় → `2 / 10 = 0.2` → `Math.floor(0.2) = 0`
* যদি `value = 15` হয় → `15 / 10 = 1.5` → `Math.floor(1.5) = 1`
* যদি `value = 22` হয় → `22 / 10 = 2.2` → `Math.floor(2.2) = 2`

👉 এই `0`, `1`, `2` আসলে bin নম্বর বোঝাচ্ছে!

---

### ③ `const binStart = Math.floor(value / binSize) * binSize`

এটা দিয়ে আমরা **bin-এর শুরু মান** বের করছি।

* value=2 → `(0)*10 = 0` → bin শুরু 0
* value=15 → `(1)*10 = 10` → bin শুরু 10
* value=22 → `(2)*10 = 20` → bin শুরু 20

---

### ④ `const binEnd = binStart + binSize - 1`

এখন bin-এর **শেষ মান** বের করছি।

* binStart=0 → end=9
* binStart=10 → end=19
* binStart=20 → end=29

---

### ⑤ `const binLabel = `${binStart}-${binEnd}``

এই লাইনে শুধু একটা **লেবেল তৈরি করছি**
যেমন `"0-9"`, `"10-19"`, `"20-29"` — এগুলা key হিসেবে ব্যবহার হবে।

---

### ⑥

```js
if (!bins[binLabel]) {
  bins[binLabel] = [];
}
```

👉 এখানে আমরা চেক করছি, এই bin আগে তৈরি হয়েছে কি না।

* যদি না হয়ে থাকে — তাহলে একটা ফাঁকা array তৈরি করো।
* মানে প্রথমবার যখন `"0-9"` bin আসে, তখন একটা array তৈরি হয়:
  `{ "0-9": [] }`

---

### ⑦

```js
bins[binLabel].push(value);
```

👉 তারপর ঐ bin-এ value যোগ করছি।

যেমন:

* 2 আসলে → `"0-9"` bin এ push হবে → `{ "0-9": [2] }`
* 5 আসলে → `"0-9"` bin এ push হবে → `{ "0-9": [2, 5] }`
* 12 আসলে → `"10-19"` bin এ যাবে → `{ "10-19": [12] }`

---

### 🔚 Final Output:

```js
{
  "0-9": [2, 5, 8],
  "10-19": [12, 15, 18],
  "20-29": [22]
}
```

---

চাও আমি এটা একটা **চিত্র (diagram)** আকারে বোঝাই — যেখানে প্রতিটা value arrow দিয়ে bin এ যাচ্ছে (visual style e)?
তাহলে logicটা একদম clear হয়ে যাবে।

*/